<scenario name="teardown" description="Environment teardown">
    <group name="Teardown">
        <!-- Do not teardown in CircleCI so we can dump onos logs-->
        <step name="Docker-Compose-Down" exec="docker-compose down -t0" unless="${CIRCLECI}"/>
    </group>
</scenario>